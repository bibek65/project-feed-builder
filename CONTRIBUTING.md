# Contributing

Add one manifest per project under `examples/projects/` and keep the schema small. Run the test and build commands before opening a pull request. Generated output belongs in build artifacts rather than source commits.
