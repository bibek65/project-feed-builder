{
  "name": "project-feed-builder",
  "version": "0.4.0",
  "private": false,
  "type": "module",
  "description": "Generate and publish project metadata for developer sites",
  "bin": { "project-feed": "dist/cli.js" },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "tsc --noEmit",
    "test": "tsx --test tests/*.test.ts",
    "feed": "tsx src/cli.ts",
    "preview:bundle": "node scripts/build-preview-bundle.mjs"
  },
  "dependencies": { "yaml": "^2.7.0" },
  "devDependencies": {
    "@types/node": "^22.15.0",
    "tsx": "^4.19.3",
    "typescript": "^5.8.3"
  }
}
